// element1 element2   选择器  element2是element1的内部元素
// element1 > element2 选择器  element2必须是element1的直接子元素

// body >header 里面可以看到容器布局的三大属性
// overflow hidden 表示内容超出范围时隐藏掉
// position relative 相对于同层元素自动布
// display block 此属性非常多，block将元素视为块，前后加入换行符

body
  >header
    width 100%
    box-shadow 2px 4px 5px hsla(0, 1%, 1%, .1)
    background color-theme
    color color-white
    padding 1em 0 0.8em
    @media tablet
      padding 1.8em 0 1.5em
    >div
      width 95%
      margin 0 auto
      position relative
      overflow hidden
      @media tablet
        width 93%
    a
      display block
      color color-white

#imglogo
  float center
  text-align center
  img
    margin auto
    //width 18em
    @media mini
      //width @width+1
    @media tablet
      //width @width+1.5


// h1.site-name 表示选择标签类型为h1,而且class为site-name 

// float 是最重要的布局元素之一
// float 会生成一个块级元素
// 如果行空间不足，它会自动跳到下一行
// position必须设置为relative，否则float设置会被破坏
// 另外relative的top left等数值设置会叠加到float上面

// float的元素位置只会影响其他float元素
// 默认position的元素不会影响float元素定位


// 这里注意下，navbar被显示在下一行是被textlogo挤下去的
// 这里的75%很讨巧

#textlogo
  float left
  width 75%
  margin-left 0.5em
  h1.site-name
    width 86%
    if ShowCustomFont
      font-family font-custom-family
      font-size font-size+100
      line-height line-height
      font-smoothing()
      @media mini
        font-size font-size+140
      @media tablet
        font-size font-size+180
    else 
      font-family font-default
      font-size font-size+60
      line-height line-height+0.4
      font-weight normal
      @media mini
        font-size font-size+90
      @media tablet
        font-size font-size+120
  h2.blog-motto
    font-size 0.7em
    font-weight normal
    @media mini
      font-size font-size
    @media tablet
      font-size font-size+10

// display.none 配合当前页面尺寸来控制显隐性，是屏幕适配的思路

.navbar
  position absolute
  width 2em
  right 0em
  top 1em
  padding 0.5em
  @media tablet
    display none

.navbutton::before
  font-family font-icon-family
  font-size 1.5em
  font-smoothing()
  content "\f0c9" 

.navmobile::before
  padding-left 0.5em

header nav
  float left
  width 100%
  @media phone
    width 50%
  @media tablet
    float left
    width auto
    margin-top 1em
    max-height none
  font-size font-size+12.5
  padding-top 0.5em
  max-height 0.01em
  transition max-height 1s ease-out
  ul
    @media tablet
      float left
  ul li
    @media tablet
      float left
  ul li a
    padding 0.2em 0 0.2em 1em
    @media tablet
      padding 0.2em 1.5em
    &:hover
      background saturate(darken(color-theme, 25%), 10%)
      color color-font-nav

.shownav
  max-height 40em

.search
  padding 0.1em 0 0 1em
  input
    line-height line-height+0.2
    border 1px solid color-white
    color color-white
    background transparent
    width 80%
    padding-left 0.5em
    @media tablet
      width 8em
      transition .5s width
      &:focus
        width 15em
  label
    display none